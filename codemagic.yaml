echo "name: Nemesis AI Build
workflows:
  ios-workflow:
    instance_type: mac_mini_m1
    environment:
      ios_signing:
        distribution_type: development
        bundle_id: com.Nemesis.AI
      node: 18
    scripts:
      - npm install
      - npx eas-cli build --platform ios --profile preview --non-interactive
    artifacts:
      - build/**/*.ipa" > codemagic.yaml
